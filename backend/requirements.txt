# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP client. Must be >=0.26 (the version that ADDED the `proxy` kwarg to
# httpx.Client) because supabase 2.3.4's transitive deps (gotrue/postgrest/etc.)
# pass `proxy=` into httpx.Client — with httpx 0.25.2 every create_client() call
# threw "Client.__init__() got an unexpected keyword argument 'proxy'", so the
# backend silently failed ALL Supabase writes (token persistence, job results,
# profile_requests, api_costs). Kept <0.28 so the deprecated `proxies` kwarg is
# still accepted too. Satisfies anthropic (>=0.25 for socket_options).
httpx==0.27.2

# GraphQL client (for Railway API)
gql==3.5.0
gql[httpx]==3.5.0

# Supabase client
supabase==2.3.4

# LLM integrations
openai==1.10.0
anthropic>=0.40.0

# Slide rendering (v3.1 PPTX pipeline — Stage 6)
python-pptx==0.6.23
openpyxl==3.1.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-mock==3.12.0

# Development
python-dotenv==1.0.1
