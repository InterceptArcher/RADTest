# Worker-specific requirements
# httpx must be >=0.26 (supabase 2.3.4's transitive deps pass proxy= into
# httpx.Client) and <0.28 (anthropic relies on the deprecated proxies kwarg).
# Pinned to 0.27.2 to match backend/requirements.txt — a split pin previously left
# the running build on a broken httpx and silently failed ALL Supabase writes.
httpx==0.27.2
openai==1.10.0
# 2.9.1 requires httpx>=0.26 (matches proxy= kwarg); 2.3.4 capped httpx<0.26.
supabase==2.9.1
python-dotenv==1.0.1
