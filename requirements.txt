# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# HTTP client
# Must be >=0.26 (supabase 2.3.4 transitive deps pass proxy= to httpx.Client) and
# <0.28 (anthropic uses the deprecated proxies kwarg). Pinned to 0.27.2 to match
# backend/requirements.txt and backend/worker/requirements.txt — a split pin left
# the deployed build on a broken httpx and silently failed every Supabase write.
httpx==0.27.2

# GraphQL client (for Railway API)
gql==3.5.0
gql[httpx]==3.5.0

# Supabase client. 2.9.1 requires httpx>=0.26 (matches the proxy= kwarg its
# sub-deps emit); 2.3.4 capped httpx<0.26 and conflicted with the 0.27.2 pin.
supabase==2.9.1

# LLM integrations
openai==1.10.0

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-mock==3.12.0

# Development
python-dotenv==1.0.1
