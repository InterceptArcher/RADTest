# Frontend Environment Variables
# IMPORTANT: This file is an example. Never commit actual values.
# Copy this file to .env.local and fill in the values.

# Backend API URL
# This value must be provided via environment variables
NEXT_PUBLIC_API_URL=http://localhost:8000

# For production (Vercel)
# Set this in Vercel dashboard under Environment Variables
# NEXT_PUBLIC_API_URL=https://your-backend.railway.app

# ZoomInfo OAuth 2.0 (PKCE Flow)
# These values must be provided via environment variables (Vercel dashboard)
# Obtain from ZoomInfo Developer Portal: https://developer.zoominfo.com
ZOOMINFO_CLIENT_ID=
ZOOMINFO_CLIENT_SECRET=
# Optional: space-separated scopes (defaults to "openid")
# ZOOMINFO_SCOPES=openid zi_api
